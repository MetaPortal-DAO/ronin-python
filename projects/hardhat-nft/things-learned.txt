1. Use pinata via the SDK - initialize Pinata object via pinataSDK(key, secretkey)
- create read stream using fs.createReadStream(filepath)
- pinata.pinFiletoIPFS(readableFileStream)

2. upload images AND the metadata
- construct metadata as template in deploy script 
- use response from storing images to IPFS to populate object params

3. RandomNFT
- use chainlink VRF and then an array of nums to randomly set URI

4. SVG (everything on chain)
- create variable string via abi.encodepacked (for string concat) 
- or just use new string concat method in solidity 